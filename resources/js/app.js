import './bootstrap';

// Livewire 3 bundles and manages Alpine.js internally.
// Do NOT import Alpine here — double-loading breaks all wire:click and x-data directives.
// Livewire is initialized via @livewireScripts in the layout, which also injects
// the correct update endpoint for subdirectory installs.
